import React from 'react';
import { Button } from 'react-bootstrap';

const SimpleButton = (): JSX.Element => {
    return <Button variant='outline-primary' size='lg'>Submit</Button>;
}

export default SimpleButton;